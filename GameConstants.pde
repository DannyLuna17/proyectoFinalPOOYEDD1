/**
 * GameConstants.pde
 * 
 * Define todas las constantes de estado y valores de configuración usados
 * en el juego EcoRunner. Centraliza la configuración para facilitar actualizaciones.
 */

// Estados del juego
final int STATE_INTRO_VIDEO = -1;
final int STATE_MAIN_MENU = 0;
final int STATE_INSTRUCTIONS = 1;
final int STATE_SETTINGS = 2;
final int STATE_GAME = 3;
final int STATE_GAME_OVER = 4;
final int STATE_PAUSED = 5;
final int STATE_ACCESSIBILITY = 6;
final int STATE_LEADERBOARD = 7;
final int STATE_NAME_INPUT = 8;
final int STATE_XP_SUMMARY = 9; // Pantalla de resumen de XP que se muestra después del leaderboard

// Dimensiones predeterminadas de la ventana
final int WINDOW_WIDTH = 1280;
final int WINDOW_HEIGHT = 720; 